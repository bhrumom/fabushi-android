package com.getcapacitor.myapp;

import static org.junit.Assert.assertTrue;
import org.junit.Test;

/** Legacy generated test path retained only because the connector cannot delete files. */
public final class ExampleInstrumentedTest {
    @Test
    public void legacyGeneratedPathIsInert() {
        assertTrue(true);
    }
}
